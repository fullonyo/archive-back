# ======================================
# REDIS CACHE CONFIGURATION
# ======================================

# Redis URL para desenvolvimento local (opcional)
# Se não configurado, usará cache in-memory como fallback
# REDIS_URL=redis://localhost:6379

# Redis URL para produção (obrigatório em produção)
# Exemplos de configuração para provedores populares:

# Redis Cloud
# REDIS_URL=redis://username:password@host:port

# AWS ElastiCache
# REDIS_URL=redis://your-elasticache-cluster.cache.amazonaws.com:6379

# Heroku Redis
# REDIS_URL=redis://h:password@hostname:port

# DigitalOcean Managed Redis
# REDIS_URL=rediss://username:password@host:port

# Railway Redis
# REDIS_URL=redis://default:password@host:port

# Configurações adicionais (opcionais)
REDIS_TTL_DEFAULT=300          # TTL padrão em segundos (5 minutos)
REDIS_TTL_CATEGORIES=1800      # TTL para categorias (30 minutos)
REDIS_TTL_ASSETS=300           # TTL para assets (5 minutos)
REDIS_TTL_STATS=300            # TTL para estatísticas (5 minutos)
REDIS_TTL_SEARCH=180           # TTL para busca (3 minutos)

# Para desenvolvimento local com Docker:
# docker run -d -p 6379:6379 --name redis redis:alpine
# REDIS_URL=redis://localhost:6379

# ======================================
# CACHE STRATEGY CONFIGURATION
# ======================================

# Estratégia de cache (redis|memory|hybrid)
CACHE_STRATEGY=hybrid          # hybrid usa Redis em produção, memory em desenvolvimento

# Máximo de chaves no cache in-memory (fallback)
CACHE_MAX_KEYS=1000

# TTL máximo para cache in-memory (segundos)
CACHE_MAX_TTL=3600
